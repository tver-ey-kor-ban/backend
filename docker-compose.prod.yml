# version: '3.8'

# services:
#   db:
#     image: postgres:15-alpine
#     container_name: garage_db
#     restart: always
#     environment:
#       POSTGRES_USER: ${POSTGRES_USER:-postgres}
#       POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
#       POSTGRES_DB: ${POSTGRES_DB:-garage_db}
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     networks:
#       - garage_network
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
#       interval: 5s
#       timeout: 5s
#       retries: 5

#   api:
#     build: .
#     container_name: garage_api
#     restart: always
#     command: >
#       sh -c "
#         sleep 10 &&
#         uvicorn app.main:app 
#         --host 0.0.0.0 
#         --port 8000 
#         --workers ${WORKERS:-4}
#         --proxy-headers
#       "
#     ports:
#       - "8000:8000"
#     env_file:
#       - .env
#     environment:
#       - DATABASE_URL=postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-changeme}@db:5432/${POSTGRES_DB:-garage_db}
#     depends_on:
#       db:
#         condition: service_healthy
#     networks:
#       - garage_network
#     volumes:
#       - ./uploads:/code/uploads  # For product images

#   # Optional: Nginx reverse proxy for SSL
#   nginx:
#     image: nginx:alpine
#     container_name: garage_nginx
#     restart: always
#     ports:
#       - "80:80"
#       - "443:443"
#     volumes:
#       - ./nginx.conf:/etc/nginx/nginx.conf:ro
#       - ./ssl:/etc/nginx/ssl:ro  # Mount SSL certificates
#     depends_on:
#       - api
#     networks:
#       - garage_network

# volumes:
#   postgres_data:

# networks:
#   garage_network:
#     driver: bridge
